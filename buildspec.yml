version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install

  build:
    commands:
      - echo Build phase...
      - echo No build needed for simple Node.js app

artifacts:
  files:
    - '**/*'
